import { Component } from '@angular/core';
@Component({ selector: 'app-schedule', standalone: true, template: '<p class="p-10 font-body">Weekly Schedule — being implemented</p>' })
export class ScheduleComponent {}
